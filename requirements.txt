# Live 2.0 Backend Dependencies
# Core simulation
taichi==1.6.0
numpy>=1.24.0
scipy>=1.10.0

# API and web
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0

# Data handling
msgpack>=1.0.7
h5py>=3.10.0
networkx>=3.0
psutil>=5.9.0

# Visualization
matplotlib>=3.7.0
pandas>=2.0.0

# Machine Learning (Week 4.1)
scikit-learn>=1.3.0

# Chemistry matcher (PubChem integration)
rdkit>=2024.0.0  # Use 'rdkit' package (works on Windows), not 'rdkit-pypi'
requests>=2.32.0
Pillow>=10.4.0
watchdog>=4.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
hypothesis>=6.92.0

# Development
black>=23.0.0
isort>=5.12.0
mypy>=1.7.0

# Billing and subscriptions (1.3)
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0  # PostgreSQL driver
stripe>=7.0.0  # Stripe payment processing
bcrypt>=4.0.0  # Password hashing
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing utilities
redis>=5.0.0  # Redis client (if not already included)
python-dotenv>=1.0.0  # Load environment variables from .env file
psycopg2-binary>=2.9.0  # PostgreSQL driver (already listed above, but ensuring it's here)
boto3>=1.28.0  # AWS S3 and S3-compatible storage (Supabase Storage, MinIO)