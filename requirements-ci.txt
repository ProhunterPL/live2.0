# Live 2.0 CI/CD Dependencies (lightweight version)
# Use this for GitHub Actions to avoid heavy packages

# Core simulation
taichi==1.6.0
numpy>=1.24.0
scipy>=1.10.0

# API and web
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0
httpx>=0.24.0  # Required for FastAPI TestClient

# Data handling
msgpack>=1.0.7
h5py>=3.10.0
networkx>=3.0
psutil>=5.9.0

# Visualization (lighter versions)
matplotlib>=3.7.0
pandas>=2.0.0

# Machine Learning
scikit-learn>=1.3.0

# Testing (essential)
pytest>=7.4.0
pytest-asyncio>=0.21.0
hypothesis>=6.92.0

# Development tools
black>=23.0.0
isort>=5.12.0
mypy>=1.7.0

# Chemistry - RDKit excluded (install separately if needed)
# rdkit>=2024.0.0
requests>=2.32.0
Pillow>=10.4.0
watchdog>=4.0.0

# Billing and subscriptions (required for billing tests)
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0  # PostgreSQL driver
stripe>=7.0.0  # Stripe payment processing
bcrypt>=4.0.0  # Password hashing
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing utilities
redis>=5.0.0  # Redis client
python-dotenv>=1.0.0  # Load environment variables from .env file
boto3>=1.28.0  # AWS S3 and S3-compatible storage
email-validator>=2.0.0  # Required for Pydantic EmailStr validation

# Note: RDKit can be installed separately with:
# pip install rdkit
# This is excluded from CI by default as it's large and not needed for core tests

